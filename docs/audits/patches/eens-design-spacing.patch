diff --git a/src/components/Header.astro b/src/components/Header.astro
index 00c1b6b..1a9d5e8 100644
--- a/src/components/Header.astro
+++ b/src/components/Header.astro
@@ -142,7 +142,7 @@ const menuGroups: MenuGroup[] = [
 			<p class="mb-2 font-mono text-[11px] font-medium uppercase tracking-[0.4px] text-primary">Eens property register</p>
 			<div class="space-y-1">
 				{menuGroups.map((group) => (
-					<details class="border-b border-border/60 last:border-b-0">
+					<details class="group border-b border-border/60 last:border-b-0">
 						<summary class="flex min-h-12 cursor-pointer list-none items-center justify-between py-3 text-sm font-medium text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50 [&::-webkit-details-marker]:hidden">
 							<span>{group.label}</span>
 							<Icon name="chevron-down" class="size-4 text-muted-foreground transition-transform duration-200 group-open:rotate-180" />
diff --git a/src/components/blocks/PropertyCard.astro b/src/components/blocks/PropertyCard.astro
index 5d148b3..28d993c 100644
--- a/src/components/blocks/PropertyCard.astro
+++ b/src/components/blocks/PropertyCard.astro
@@ -26,6 +26,7 @@ export interface PropertyCardData {
 	title?: string | null;
 	availability?: Availability | null;
 	address?: string | null;
+	development?: string | null;
 	sqft?: string | null;
 	price?: { ksh?: string | null; perSqft?: string | null } | null;
 	zone?: string | null;
@@ -90,7 +91,7 @@ const sourceUrl = getPexelsSource(resolvedImageSrc);
 				)}
 			</div>
 		)}
-	<div class="flex flex-1 flex-col gap-3 p-5">
+	<div class="flex flex-1 flex-col gap-4 p-6">
 		{(data.type || data.availability) && (
 			<div class="flex flex-wrap items-center justify-between gap-2">
 				{data.type && (
@@ -113,6 +114,12 @@ const sourceUrl = getPexelsSource(resolvedImageSrc);
 				{data.address}
 			</p>
 		)}
+		{data.development && (
+			<div data-tina-field={tinaField(data as { development?: unknown }, 'development')} class="flex items-center gap-2 font-mono text-[12px] text-muted-foreground">
+				<Icon name="building-community" aria-hidden="true" class="size-3.5 shrink-0 text-primary" />
+				<span>{data.development}</span>
+			</div>
+		)}
 		{data.sqft && (
 			<div data-tina-field={tinaField(data as { sqft?: unknown }, 'sqft')} class="font-mono tabular-nums text-[13px] text-foreground/80">
 				{data.sqft}
@@ -132,7 +139,7 @@ const sourceUrl = getPexelsSource(resolvedImageSrc);
 			<a href={sourceUrl} target="_blank" rel="noopener noreferrer" class="inline-flex min-h-10 items-center self-start rounded-sm font-mono text-[11px] text-muted-foreground underline-offset-2 hover:text-foreground hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50">Photo source</a>
 		)}
 		{(data.zone || data.leaseTerm) && (
-			<div class="flex flex-wrap items-center gap-2 pt-1">
+			<div class="flex flex-wrap items-center gap-2 pt-2">
 				{data.zone && (
 					<div data-tina-field={tinaField(data as { zone?: unknown }, 'zone')}>
 						<LocationPin zone={data.zone} mapHref={mapHref} />
@@ -146,7 +153,7 @@ const sourceUrl = getPexelsSource(resolvedImageSrc);
 			</div>
 		)}
 		{viewHref && (
-			<div class="mt-auto flex items-center justify-end pt-2">
+			<div class="mt-auto flex items-center justify-end pt-4">
 				<Button href={viewHref} variant="secondary" size="sm">
 					<span>{viewLabel}</span>
 					<Icon name="arrow-up-right" class="size-3.5" />
diff --git a/src/components/blocks/PropertyList.astro b/src/components/blocks/PropertyList.astro
index 1150846..51ffb4d 100644
--- a/src/components/blocks/PropertyList.astro
+++ b/src/components/blocks/PropertyList.astro
@@ -49,7 +49,7 @@ const filtered: PropertyNode[] = all
 <Section>
 	<div class="mx-auto w-full">
 		{(data.title || data.description) && (
-			<header class="mx-auto mb-10 max-w-3xl text-center md:text-left">
+			<header class="mx-auto mb-8 max-w-3xl text-center md:mb-10 md:text-left">
 				{data.title && (
 					<h2 data-tina-field={tinaField(data as { title?: unknown }, 'title')} class="font-display text-balance text-3xl font-semibold tracking-[-0.6px] md:text-4xl md:tracking-[-1.0px]">
 						{data.title}
@@ -62,7 +62,7 @@ const filtered: PropertyNode[] = all
 				)}
 			</header>
 		)}
-		<div class:list={[data.layout === 'strip' ? 'flex flex-col gap-6' : 'grid gap-6 sm:grid-cols-2 lg:grid-cols-3']}>
+		<div class:list={[data.layout === 'strip' ? 'flex flex-col gap-6' : 'grid gap-8 sm:grid-cols-2 lg:grid-cols-3']}>
 			{filtered.map((p: PropertyNode) => (
 				<PropertyCardFeatured
 					data={{
@@ -83,7 +83,7 @@ const filtered: PropertyNode[] = all
 			))}
 		</div>
 		{blockActions.length > 0 && (
-			<div class="mt-10 flex flex-wrap items-center justify-center gap-3">
+			<div class="mt-8 flex flex-wrap items-center justify-center gap-3 md:mt-10">
 				{blockActions.map((action: Action) => (
 					<Button href={action.link} variant={action.type === 'link' ? 'ghost' : 'default'} size="lg">
 						{action.icon && <Icon name={action.icon} class="size-4" />}
diff --git a/src/components/ui/Section.astro b/src/components/ui/Section.astro
index b3b113d..60e06c5 100644
--- a/src/components/ui/Section.astro
+++ b/src/components/ui/Section.astro
@@ -9,7 +9,7 @@ type Props = HTMLAttributes<'section'> & {
 const { background = 'bg-default', class: className, ...rest } = Astro.props;
 ---
 <div class={background}>
-	<section class={cn('mx-auto w-full max-w-7xl px-6 py-16 md:py-section', className)} {...rest}>
+	<section class={cn('mx-auto w-full max-w-7xl scroll-mt-20 px-4 py-14 sm:px-6 md:py-section lg:px-8', className)} {...rest}>
 		<slot />
 	</section>
 </div>
diff --git a/src/pages/[category]/index.astro b/src/pages/[category]/index.astro
index c9fd4f2..9083fb1 100644
--- a/src/pages/[category]/index.astro
+++ b/src/pages/[category]/index.astro
@@ -61,16 +61,16 @@ const viewingHref = `mailto:${contactEmail(config)}?subject=${encodeURIComponent
 		</Section>
 	)}
 
-	<Section class="bg-background pt-0">
+	<Section class="bg-background">
 		<div class="mx-auto max-w-7xl">
-			<header class="mb-8 flex flex-wrap items-end justify-between gap-4">
+			<header class="mb-8 flex flex-wrap items-end justify-between gap-4 md:mb-10">
 				<div>
 					<h2 class="font-display text-2xl font-semibold tracking-[-0.4px]">Published {definition.label.toLowerCase()}</h2>
 				</div>
 				<span class="font-mono tabular-nums text-[13px] text-muted-foreground">{listings.length} record{listings.length === 1 ? '' : 's'}</span>
 			</header>
 			{listings.length > 0 ? (
-				<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
+				<div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
 					{listings.map((property) => (
 						<PropertyCardFeatured
 							data={{
@@ -78,6 +78,7 @@ const viewingHref = `mailto:${contactEmail(config)}?subject=${encodeURIComponent
 								title: property.title ?? null,
 								availability: (property.availability as Availability | undefined) ?? null,
 								address: property.address ?? null,
+								development: property.development ?? null,
 								sqft: property.sqft ?? null,
 								price: property.price ?? null,
 								zone: property.zone ?? null,
@@ -91,8 +92,9 @@ const viewingHref = `mailto:${contactEmail(config)}?subject=${encodeURIComponent
 					))}
 				</div>
 			) : (
-				<div class="max-w-2xl rounded-xl border border-hairline-steel bg-secondary p-8">
-					<h2 class="font-display text-2xl font-medium tracking-[-0.4px]">{definition.emptyTitle}</h2>
+				<div class="relative max-w-2xl overflow-hidden rounded-xl border border-hairline-steel border-l-4 border-l-primary bg-secondary p-6 sm:p-8">
+					<p class="font-mono text-[12px] text-primary">Register status</p>
+					<h2 class="mt-3 font-display text-2xl font-medium tracking-[-0.4px]">{definition.emptyTitle}</h2>
 					<p class="mt-3 text-muted-foreground">{definition.emptyDescription}</p>
 					<div class="mt-6 flex flex-wrap gap-3">
 						<Button href={viewingHref} size="lg">Ask about availability</Button>
diff --git a/src/styles/global.css b/src/styles/global.css
index 3bbb6b9..cdc2d5b 100644
--- a/src/styles/global.css
+++ b/src/styles/global.css
@@ -171,7 +171,7 @@
 		color: var(--foreground);
 		font-family: var(--font-sans);
 		margin: 0;
-		padding-top: 5rem;                          /* sticky header compensation */
+		padding-top: 4rem;                          /* sticky header compensation */
 	}
 	.bg-default {
 		background-color: var(--background);
diff --git a/tina/collections/property.ts b/tina/collections/property.ts
index 28cdf75..8ce120b 100644
--- a/tina/collections/property.ts
+++ b/tina/collections/property.ts
@@ -72,6 +72,12 @@ export const PropertyCollection: Collection = {
 			description: 'Shown on the listing card and as the page H1. Always include the zone + identifier.',
 		},
 		{ name: 'type', label: 'Type', type: 'string', options: TYPE_OPTIONS, required: true },
+		{
+			name: 'development',
+			label: 'Development / park (optional)',
+			type: 'string',
+			description: 'Use only when an operator-approved record identifies the named development or park. Leave blank when compound membership is unknown.',
+		},
 		{ name: 'availability', label: 'Availability', type: 'string', options: AVAILABILITY_OPTIONS, required: true },
 		{
 			name: 'address',
