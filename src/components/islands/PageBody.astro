---
import Blocks from '../blocks/Blocks.astro';
import type { CmsPage } from '../../lib/data';

interface Props {
	data?: CmsPage | null;
	slug?: string;
}

const { data, slug = '' } = Astro.props;
---

{data && <Blocks data={data} slug={slug} />}
