{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build:local || exit 1; pnpm build:search || echo warning: search index sync failed (non-fatal) -- see Vercel build log"
}
